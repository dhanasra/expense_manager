import 'package:flutter/material.dart';

class CashEntryFieldSettingsPage extends StatelessWidget {
  const CashEntryFieldSettingsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MainBody extends StatelessWidget {
  const MainBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
